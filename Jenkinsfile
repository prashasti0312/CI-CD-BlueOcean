pipeline {
      agent any
       stages {
           stage("Build"){
            steps{
              echo "Build Stage"
                  }    
                           }
           stage("Test"){
            steps{
              echo "Test"
                  }    
                           }
            stage("Clean"){
            steps{
              echo "Clean Stage"
                  }    
                           }

              }
    }
